# Data directory

Place source files under `data/raw/` and converted interchange files under
`data/normalized/`. Both are ignored by Git. Do not commit third-party datasets, access
tokens, personal data, or restricted flight logs.
